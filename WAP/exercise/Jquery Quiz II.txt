1.
	Yes, but the function is the parameter of each() methon,
	so they have parameters of same type.
2.
	$("ul").parent().css("color","blue");
3.
	$("body").append($("<div>"));
	$("div").append($("<h1>"));
	$("h1").append("JQuery Core");